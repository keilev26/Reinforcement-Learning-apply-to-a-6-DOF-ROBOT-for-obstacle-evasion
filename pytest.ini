[pytest]
pythonpath = .
testpaths = gym_env/tests
# Los plugins de pytest de ROS 2 se cargan solos si el entorno de ROS está activo, y fallan
addopts = -p no:launch_testing -p no:launch_testing_ros_pytest_entrypoint -p no:launch_ros
